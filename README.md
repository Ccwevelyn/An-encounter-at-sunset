# 日落相遇（An encounter at sunset）

校内恋爱匹配小站：@mpu.edu.mo 邮箱注册，填写档案与灵魂共鸣主观题，可进行随机匹配、缘分匹配、灵魂共鸣匹配，与匹配对象聊天。含三个聊天角色（最伟大最尊敬的导师 / 看不上你对象的朋友 / 知心姐姐）可闲聊。

---

## 匹配机制

所有匹配（随机 / 缘分 / 灵魂共鸣）都遵循：**仅会匹配到「双向符合性别偏好」的人**。

### 随机匹配

- 可选择**是否开启随机匹配状态**；选择开启后即进入候选池，即可被匹配到。
- **双向性别偏好**过滤后，在剩余用户中**随机**选一人匹配。
- 若当前没有开启随机匹配且符合性别偏好的用户，会提示「暂无开启随机匹配且符合性别偏好的用户」。

### 缘分匹配

1. **性别**：同上，只保留双向符合性别偏好的用户。
2. **恋爱目的**  
   - 若你选「专注当下的快乐」：只与同样选「专注当下的快乐」的人匹配。  
   - 若你选「走向未来的婚姻」：只与同样倾向婚姻的人匹配，且需满足：  
     - **城市**：双方填写的城市至少有一个相同；  
     - **月花销档位**：按 A/B/C/D/E 档位，相邻档位可匹配（如 B 可匹配 A/B/C）。
3. **想恋爱指数**：两人指数差值 ≤ 2 才保留。
4. **恋爱经历（段数）**：两人经历档位差值 ≤ 2 才保留。
5. **优先级（在满足上述条件后）**  
   - 优先**同学位**；  
   - 再优先**最长恋爱时长**相近（档位差值 ≤ 2）；  
   - 再优先 **MBTI** 同类型或设定中的经典配对。  
6. 在最终候选池中**随机**选一人作为缘分匹配结果。

### 灵魂共鸣匹配

- 仅对**已填写灵魂共鸣主观题**的用户开放；未填时点击会提示先填写。
- 在已答题且**双向符合性别偏好**的用户中，若有配置 DeepSeek，则按双方答案契合度排序后匹配；否则在候选池中随机一人。

---

## 功能概览

- **登录 / 注册**：邮箱须为 `@mpu.edu.mo`。注册需先获取邮箱验证码再填写昵称与密码；登录支持密码与验证码（4 位、5 分钟有效）。
- **档案**：性别、希望匹配的性别、学院、专业、生日、MBTI、恋爱经历、恋爱目的、城市、月花销、想恋爱指数等。
- **匹配**  
  - **随机匹配**：开启随机匹配状态后，在符合双向性别偏好的用户中随机一人。  
  - **缘分匹配**：按恋爱目的、城市、花销档位、想恋爱指数、经历、学位、MBTI 等规则筛选后择优随机。  
  - **灵魂共鸣**：5 道主观题一道一道作答后保存，在已答题且符合性别偏好的用户中，由 AI（DeepSeek）按契合度排序后匹配；未填主观题时点击会提示先填写。
- **聊天**：匹配列表、与对方聊天；列表前三位为三个固定角色（可与 TA 闲聊，支持 AI 回复）：**最伟大最尊敬的导师**（点拨引领、拓展思考）、**看不上你对象的朋友**（总嫌你对象配不上你、爱吐槽）、**知心姐姐**（温柔和蔼），需配置 DeepSeek 时由 AI 生成回复。
- **数据库**：支持 SQLite（默认）与 PostgreSQL（设置 `DATABASE_URL` 时自动切换）。

## 关于三个聊天角色（导师 / 看不上你对象的朋友 / 知心姐姐）

三个角色均为**固定系统提示（system prompt）**驱动，没有用你的聊天记录训练模型。配置了 `DEEPSEEK_API_KEY` 后，每次发消息会把对应角色的提示 + 你的话发给 DeepSeek 生成回复；未配置时使用内置简短规则回复。角色设定：**最伟大最尊敬的导师**庄严智慧、善于点拨与拓展思考；**看不上你对象的朋友**总嫌你对象配不上你、爱吐槽泼冷水但本质关心你；**知心姐姐**温柔、包容、善解人意。

## 查看数据库

- **本地用 SQLite 时**：在浏览器打开 `/admin?secret=你的ADMIN_SECRET`（环境变量 `ADMIN_SECRET` 需与 URL 中 `secret` 一致），可查看/编辑 users、profiles、matches、messages 等表。
- **部署用 PostgreSQL 时**：不提供 `/admin`，在 Render（或所用平台）控制台里打开该 PostgreSQL 实例，用其自带的数据库管理或外部客户端连接 `DATABASE_URL` 查看。

## 文件架构

```
├── client/                 # 前端（React + Vite）
│   ├── src/
│   │   ├── api.js          # 接口封装
│   │   ├── App.jsx
│   │   ├── main.jsx、index.css
│   │   ├── components/     # 日期、地图等组件
│   │   ├── data/           # 城市、MBTI、省份等静态数据
│   │   └── pages/          # 页面组件（登录、注册、首页、个人、聊天、灵魂共鸣等）
│   ├── index.html、vite.config.js
│   └── package.json
├── server/                 # 后端（Node + Express）
│   ├── index.js            # 入口，挂载路由与静态
│   ├── db.js、db-pg.js     # SQLite / PostgreSQL
│   ├── deepseek.js
│   ├── middleware/auth.js
│   ├── routes/             # auth、profile、match、chat、admin
│   ├── admin.html          # 本地数据库管理页
│   └── package.json
├── package.json            # 根脚本 install:all、build、start
├── README.md、DEPLOY.md
└── .gitignore
```

## 页面布局（路由）

| 路径 | 说明 |
|------|------|
| `/` | 首页：匹配入口（随机 / 缘分 / 灵魂共鸣）、聊天与个人页入口 |
| `/login` | 登录（密码 / 验证码） |
| `/register` | 注册（邮箱验证码 + 昵称 + 密码） |
| `/onboarding` | 新用户完善档案（多步问卷） |
| `/profile` | 个人页面：展示与编辑资料 |
| `/chats` | 聊天列表：三个固定角色 + 匹配到的用户 |
| `/chat/:partnerId` | 与某人或某角色对话 |
| `/match/:partnerId` | 匹配结果页：对方档案与进入聊天 |
| `/soul` | 灵魂共鸣：主观题一道一道作答并保存 |
| `/author` | 作者的话 |

未登录访问需登录页的会跳转 `/login`；未完善档案会先走 `/onboarding`。

## 技术栈

- 前端：React 18、React Router、Vite  
- 后端：Node.js、Express、SQLite / PostgreSQL、DeepSeek API（可选）
